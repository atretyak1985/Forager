services:
  searxng:
    image: searxng/searxng:latest
    container_name: forager-searxng
    ports:
      - "127.0.0.1:8888:8080"   # localhost only — do not expose externally
    volumes:
      - ./searxng:/etc/searxng
    environment:
      - SEARXNG_BASE_URL=http://localhost:8888/
    restart: unless-stopped
